# Bug fixing

* Mostrar imatges prod vs. dev?

* fab task to download?

* cron task

* Replace current database with: (in prod_settings template):

DATABASES = {{
    "default": {{
        # Ends with "postgresql_psycopg2", "mysql", "sqlite3" or "oracle".
        "ENGINE": "django.db.backends.postgresql_psycopg2",
        # DB name or path to database file if using sqlite3.
        "NAME": "{proj_name}",
        # Not used with sqlite3.
        "USER": "{proj_name}",
        # Not used with sqlite3.
        "PASSWORD": "{db_pwd}",
        # Set to empty string for localhost. Not used with sqlite3.
        "HOST": "127.0.0.1",
        # Set to empty string for default. Not used with sqlite3.
        "PORT": "",
    }}
}}

# Now

* Add simple AI to predict crowd count in download_and_process.py


# Future

* Use generic relations to allow different handlers for urls with specific formats.
* Add view for historic statistics (processing data.models.Prediction).

* Use statistics to set data.models.BeachCam.max_crowd_count automatically.
* Add log to script download_and_process.py